# integration tests

This directory contains integration tests for your package.

Each one will be run in a separate process, so they can be written in any language.

file names should end with `.test.*` to be picked up by the test runner.
